= form_tag("/users/#{@sell.user_id}/sells/#{@sell.id}", :method => :patch) do
  %h3
    編集する
  %input{cols: "10", :autofocus => "true", :name => "name", :placeholder => "商品名", :type => "text", :value => "#{@sell.name}"}/
  %input{cols: "6", :name => "price", :placeholder => "金額", :value => "#{@sell.price}"}/
  %input{:type => "submit", :value => "SENT"}/



-# = form_for(@sell, url: ) do |f|
-#   = f.text_field :name, placeholder: "商品名"
-#   = f.text_field :price, placeholder: "金額"
-#   = f.file_field :image
-#   = f.submit '出品', class: 'form__submit'